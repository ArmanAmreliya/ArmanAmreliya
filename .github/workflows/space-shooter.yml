name: Update Space Shooter GIF

on:
  schedule:
    - [cite_start]cron: "0 0 * * *" # Runs daily at midnight [cite: 19]
  [cite_start]workflow_dispatch: # Allows you to run it manually [cite: 20]

permissions:
  [cite_start]contents: write [cite: 22]

jobs:
  generate:
    [cite_start]runs-on: ubuntu-latest [cite: 25]
    steps:
      - [cite_start]uses: actions/checkout@v4 [cite: 27]
      
      - name: Create assets directory
        [cite_start]run: mkdir -p assets [cite: 28, 29]
        
      - [cite_start]uses: czl9707/gh-space-shooter@v1 # [cite: 6, 30]
        with:
          [cite_start]github-token: ${{ secrets.GITHUB_TOKEN }} # [cite: 32]
          [cite_start]output-path: assets/space-shooter.gif # [cite: 32]
          [cite_start]strategy: random # [cite: 32]